{
    "commands": [
        {
            "name": "set-interval",
            "parameters": [
                "seconds"
            ]
        }
    ],
    "example": "audio-classification",
    "telemetry": [],
    "notes": "TODO: define telemetry keys and commands for this app"
}
