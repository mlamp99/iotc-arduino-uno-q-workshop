{
    "commands": [
        {
            "name": "set-interval",
            "parameters": [
                "seconds"
            ]
        }
    ],
    "example": "cloud-blink",
    "telemetry": [],
    "notes": "TODO: define telemetry keys and commands for this app"
}
