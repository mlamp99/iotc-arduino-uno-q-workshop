{
    "commands": [
        {
            "name": "set-interval",
            "parameters": [
                "seconds"
            ]
        }
    ],
    "example": "anomaly-detection",
    "telemetry": [],
    "notes": "TODO: define telemetry keys and commands for this app"
}
