{
    "commands": [
        {
            "name": "set-interval",
            "parameters": [
                "seconds"
            ]
        }
    ],
    "example": "blink-with-ui",
    "telemetry": [],
    "notes": "TODO: define telemetry keys and commands for this app"
}
